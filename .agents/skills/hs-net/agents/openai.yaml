interface:
  display_name: "Hs Net"
  short_description: "Repo-specific patterns and workflows for hs-net"
  default_prompt: "Use the hs-net repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true